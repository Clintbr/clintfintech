# Copiez ce fichier vers .env à la racine du dépôt, puis renseignez les mots de passe.
# Sur Render : ajoutez AUTH_ACCOUNTS dans Environment (Secret), sans fichier .env.

# Tableau JSON sur une seule ligne : username, password, memberId, role (ADMIN | MEMBER)
AUTH_ACCOUNTS=[{"username":"admin","password":"REMPLACER","memberId":0,"role":"ADMIN"},{"username":"Clint","password":"REMPLACER","memberId":1,"role":"MEMBER"}]

# Optionnel (Docker / Render)
# NODE_ENV=production
# PORT=3000
# APP_ROOT=/app
# DATA_DIR=/app/prototyp/data
